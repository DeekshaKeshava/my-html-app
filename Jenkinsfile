pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git 'https://github.com/your-username/html-app.git'
            }
        }

        stage('Build') {
            steps {
                echo 'No build needed for HTML'
            }
        }

        stage('Test') {
            steps {
                echo 'No tests yet'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying HTML app...'
            }
        }
    }
}